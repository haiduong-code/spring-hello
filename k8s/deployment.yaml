apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-hello
  labels:
    app: spring-hello
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spring-hello
  template:
    metadata:
      labels:
        app: spring-hello
    spec:
      imagePullSecrets:
      - name: ghcr-secret # <-- tạo secret để đăng nhập GHCR (nếu cần)
      containers:
      - name: spring-hello
        image: ghcr.io/haiduong-code/spring-hello:63132e21dbb566d62c8be6def46f86482e7121c6
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080