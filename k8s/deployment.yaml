apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-hello
  labels:
    app: spring-hello
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spring-hello
  template:
    metadata:
      labels:
        app: spring-hello
    spec:
      imagePullSecrets:
      - name: ghcr-secret # <-- tạo secret để đăng nhập GHCR (nếu cần)
      containers:
      - name: spring-hello
        image: ghcr.io/haiduong-code/spring-hello:f0960060401fbbba2d8f3b49fabe75a2be06561a
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080